# Copy to manual_bridge.env (gitignored) and fill in.
# TSX_USER is your TopstepX login name; TSX_API_KEY comes from the TopstepX
# dashboard (Settings -> API). Both are only ever read by manual_bridge.py.
TSX_USER=your_topstepx_username
TSX_API_KEY=your_api_key_here
